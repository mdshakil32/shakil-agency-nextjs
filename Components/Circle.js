
const Circle = ({top, left, bottom, right, backgroundColor}) => {
    return (
        <div className="circle" style={{top, left, bottom, right, backgroundColor}}>
            
        </div>
    );
};

export default Circle;